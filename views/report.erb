<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Toogl free users reports</title>
    <%= css :application %>
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.3/css/materialize.min.css">
    <%= js :application %>
</head>
<body class="report" >
  <div class="row">
    <%= erb :navbar %>
    <div class="large-8 columns">  
      <form action="/report/date" method="post">  
        <div class="form-group">
          <input type="text" class="datepicker1" name="start_date" value="2015-10-12" >
        </div>
        <div class="form-group">
          <input type="text" class="datepicker2" name="end_date" value="2015-10-19">
        </div>
        <div class="form-group">
          <select>
            <option value="description">Description</options>
            <option value="tag">Tag</options>
          </select>
        </div>
        <br>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
    </div>
  </div>
</div>
<div class="row"> 
  <div class="small-6 large-centered columns">
      <% if @time_entries %>
        <dl>
          <% @time_entries.each do | key, value | %>
            <div>
            <dt> <%= key %> </dt> <dd data-time="<%= value %>"><%= Time.at(value).utc.strftime("%H:%M:%S") %></dd>
            </div>
          <% end %>
        </dl>
      <% end %>
  </div>
</div>

<script>

  $('.datepicker1').pickadate({
  firstDay: 1,
  formatSubmit: 'yyyy-mm-dd',
  });
$('.datepicker2').pickadate({
  firstDay: 1,
  formatSubmit: 'yyyy-mm-dd',
});
/*
var salida = $('.datepicker2').pickadate({
 formatSubmit: 'yyyy-mm-dd',
});

var entrada =   $('.datepicker1').pickadate({
 formatSubmit: 'yyyy-mm-dd',
 onSet: function(date){
   salida
     .set("min", sumDays(entrada.get(), 1) )
     .set("select", sumDays(entrada.get(), 1));
 }
});

entrada.set("select", -7); (edited)
*/
</script>
  <!-- Compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.3/js/materialize.min.js"></script>
</body>
</html>
